{% extends '::base.html.twig' %}

{% block content %}
<h1>Product: {{ product.name }} ({{ product.reference }})</h1>
{{ form_start(form) }}
{{ form_widget(form) }}
<a href="{{ path('admin_products_index') }}" class="btn btn-default">Cancel</a>
<input type="submit" value="Edit" class="btn btn-success" />
{{ form_end(form) }}
{% endblock %}

